<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="true">

    <ImageView
        android:id="@+id/top_label"
        android:layout_width="1167dp"
        android:layout_height="512dp"
        android:contentDescription="@string/top_label"
        android:rotation="-45"
        android:scaleType="center"
        android:src="@drawable/background"
        android:transformPivotX="583dp"
        android:transformPivotY="512dp"
        app:layout_constraintBottom_toTopOf="@id/guideline"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.5" />


    <ImageView
        android:id="@+id/bottom_label"
        android:layout_width="1167dp"
        android:layout_height="512dp"
        android:contentDescription="@string/bottom_label"
        android:rotation="-45"
        android:scaleType="center"
        android:src="@drawable/background"
        android:transformPivotX="583dp"
        android:transformPivotY="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/guideline" />

</androidx.constraintlayout.widget.ConstraintLayout>